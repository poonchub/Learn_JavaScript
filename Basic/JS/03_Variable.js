/*
Variable
    Dinamic Typing 
        คือ ชนิดตัวแปรที่จะเป็นอะไรก็ได้ตามค่าที่ตัวมันเก็บโดยไม่ต้องประกาศชนิดข้อมูล

    Static Typing
        คือ ชนิดตัวแปรที่ต้องประกาศชิดข้อมูลในตอนเริ่มต้น เช่น int double เพื่อบอกว่าตัวแปรนี้เก็บข้อมูลชนิดไหน

    การนิยาม
        var name;
        var name = ค่าเริ่มต้น;
        var name1 = ค่าเริ่มต้น, var name2 = ค่าเริ่มต้น;

        let name;   (ปัจจุบันใช้แทน var)
        let name = ค่าเริ่มต้น;
        let name1 = ค่าเริ่มต้น, var name2 = ค่าเริ่มต้น;

        const name = ค่าตัวแปร;   (ค่าคงที่)
*/

let st = "Happy New Year";
let age = 20;

document.write(st);
console.log(age);